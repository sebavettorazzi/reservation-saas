import { Router } from "express";
const router = Router();

// Ejemplo de endpoint GET
router.get("/", async (req, res) => {
  res.send("Lista de servicios aún no implementada");
});

export default router;